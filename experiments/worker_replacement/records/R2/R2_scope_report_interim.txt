SCOPE RUN REPORT — descriptive only

NO SIGNIFICANCE CLAIM. At 2-3 episodes per cell there is no contrast verdict in either direction — not 'channel X moved allocation', and not 'channel X did not'. These are descriptive point estimates with intervals wide enough to contain almost anything. The run exists to see behaviour and measure variance.

episodes: 8

  cell  n   mean regret share    role
  0     2   0.2995               information-absent control; allocation-visib
  1     1   0.2307               card channel (marginal)
  2     1   0.1866               declaration channel (marginal)
  3     1   0.3033               ask channel (ride-along)
  4     1   0.1869               CEILING — a bound, NOT an interaction claim
  U     2   0.3395               unswapped control — the JOINT effect of the 

REGRET SPLIT THREE WAYS (summed over 8 episodes; they sum to regret by construction):
  unstaffed loss (NON-routing)         11.6373   61.6%
  allocation loss on STAFFED units     -0.6248   -3.3%
  execution loss (signed)               7.8840   41.7%
  total regret                         18.8965   (max per-episode residual 8.88e-16)
  MIS-routing and NON-routing are different behaviours and only the first is
  plausibly channel-sensitive — which is why they are no longer one term.
  NOTE ON THE NEGATIVE ALLOCATION TERM, which is not evidence the manager
  out-allocated the oracle: the capacitated oracle DELIBERATELY UNDER-SERVES some
  segments to free capacity for others (measured on seed 3: 1 of 9). A manager that
  staffs such a segment beats the oracle's choice ON THAT SEGMENT while losing
  overall by failing to staff the ones the oracle was protecting. The split is
  exact; this term is a difference from the oracle's own attribution, not a
  per-segment optimum.

THE FOUR QUANTITIES (each with its df — at this size the precision matters
more than the point value):
  sigma pooled within cell : 0.1310  df=2
  sigma pooled STAFFED-ONLY: 0.0995  df=2
  GAP (full - staffed)     : 0.0315
    the gap IS the answer to whether the DV measures stalling: a material divergence means non-routing dominates the variance, and the staffed-only figure is the one a sizing decision uses
  sigma_d paired vs cell 0 : 0.0050  df=1
  sigma between instances  : 0.1310  df=1   (design-side prior 0.0254)

  sigma_d and sigma_between are ONE COUPLED QUESTION, not two independent inflators: effective pairing shrinks sigma_d AND promotes between-instance heterogeneity to the leading term, so the two corrections partially self-cancel.

DECLINE CHANNEL (what the unscripting was built to expose): 3 explicit declines,
  12 unreadable deliverables — never summed, different behaviours.

CONCURRENCY (an instrument setting that VARIES here): ['2', '__ABSENT__']
  concurrency varies across cells in this exploratory run; recorded rather than erased. Constant across cells for any powered study, no exceptions.

SAMPLING: the low pick is the SUITE MINIMUM (seed 23 at 0.0516) and the chosen mean 0.0938 sits below the admitted mean 0.1034, so EFFECT MAGNITUDES ARE BELOW THE SUITE'S BY CONSTRUCTION; and this compounds with the stratification inflation, which widens sigma_between by design. Harmless for sigma, not harmless for effect magnitudes.

written -> /home/therealgod/Projects/AdHocTeamwork/manager_agent_gym/experiments/worker_replacement/records/R2/scope_report.json

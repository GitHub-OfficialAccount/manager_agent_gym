R2 — six cells as configuration; only three switches may differ

1. configuration matrix vs the spec table:
   cell  swap   card_upd  decl   ask   
   U     False  False     False  False 
   0     True   False     False  False 
   1     True   True      False  False 
   2     True   False     True   False 
   3     True   False     False  True  
   4     True   True      True   True  
   [ok] all six match the spec table

2. C1 card — the stale card is the predecessor's own text:
   predecessor card: Credit risk analyst. Approved scope — IRB model approval: bank; ...
   successor in cell 0 (stale): Credit risk analyst. Approved scope — IRB model approval: bank; ...
   [ok] BYTE-IDENTICAL to the predecessor's — inherited, never written afresh
   [ok] and cell 1's updated card is the successor's own, which differs

3. C2 declaration — only the method line moves:
   [ok] cell 0: method line in prompt=False, in task=False (want False in both)
   [ok] cell 2: method line in prompt=True, in task=True (want True in both)
   [ok] cell 4: method line in prompt=True, in task=True (want True in both)
   [ok] cell 1: method line in prompt=False, in task=False (want False in both)
   [ok] the VALUE line and the DECLINE form are present in EVERY cell — scoring and
        refusal are identical, so a contrast cannot measure them

4. C3 ask — a prompt difference, NOT a tool difference:
   [ok] cell 0: manager id named in prompt = False (want False)
   [ok] cell 3: manager id named in prompt = True (want True)
   [ok] cell 4: manager id named in prompt = True (want True)
   tools in cells 0/3/4: ['broadcast_message', 'broadcast_message', 'get_conversation_with', 'get_recent_messages', 'get_recent_messages', 'get_task_messages', 'send_message', 'send_message']
   [ok] TOOLS ARE IDENTICAL across cells — the core-tool rule forbids differentiating
        by tool possession, so the ask channel cannot be built that way

5. cell U — no swap, scored against its OWN roster:
   changes applied at t_swap: 0; roster ['w_552723', 'w_6aaa50', 'w_8fcf6e']
   [ok] the predecessor never leaves
   [ok] and U is scored against the PRE-swap roster — its own attainable optimum,
        not one for a team it never had

6. residual diff — with the convention and ask blocks removed, every cell's
   prompt must be IDENTICAL (this is the check that finds what I did not name):
   STRIPPED (the full list — the claim is only as good as this):
     1. the report-convention block (C2's surface), whichever variant the cell uses
     2. the ask block (C3's surface), empty in non-ask cells
   and NOTHING ELSE. The stripper does not consult the cell name, so it cannot remove
   an unintended difference in the one cell that has it.
   distinct residual prompts across six cells: 1
   [ok] exactly one — nothing varies outside the two stripped blocks
   [ok] instance hash, task count and horizon identical across all six

7. manager-side residual diff (RR: C1 is a MANAGER-side manipulation;
   the worker check could never have covered it):
   [ok] cell 0's manager prompt carries the PREDECESSOR's card for the successor
   [ok] cell 1's carries the successor's own — so C1 is visible where the DV is made
   distinct manager residuals across six cells: 1
   [ok] exactly one — with the roster/card block removed, every cell's MANAGER
        prompt is identical. Only the agent lines were stripped; a stripper that removed
        more would hide what this check exists to find.

RESULT: PASS — the matrix matches the spec; the stale card is inherited byte-identically; scoring and refusal are identical across cells; the ask channel is prompt-level with identical tools; U carries no swap; and nothing varies outside the manipulated blocks

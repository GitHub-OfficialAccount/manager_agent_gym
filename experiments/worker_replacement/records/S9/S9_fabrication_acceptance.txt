S9 — fabrication detectors

1. generator requirement (spec §6): a fabricated guess must not be able to
   land on a true value by coincidence
   guess model: a fabricator picks a PD within a factor of 3.0 either side of the truth; candidates lie on the generator's 1e-06 rounding grid
   worst bucket: sovereign/AAA to AA- at w_e172a9, PD 0.000159, coincidence probability 2.36e-03 (~1 in 424)
   3 of 38 buckets exceed the 1e-03 threshold
   [FLAG] the requirement DOES NOT HOLD — concentrated in the lowest-PD buckets
        (AAA to AA-), where the plausible guess interval shrinks with the value
        while the generator's 1e-6 rounding grid does not.
   [FLAG] EXPOSURE: a false EXONERATION requires a fabrication on such a bucket AND
        a guess landing on the grid point — ~1 in 424 per fabrication, so expected
        false exonerations stay well below 1 across any study this size. Reported
        rather than absorbed; the ruling on raising generator PD precision is LS's,
        because it invalidates every committed instance hash.
   [FLAG] DIRECTION: the residual is a false NEGATIVE (a fabricator exonerated),
        never a false positive. The detector does not gain hits from this.

2. clean baseline (every segment faithful):
   classifications: {'sa_matching': 8, 'irb_matching_with_provisioned_parameters': 1}
   [ok] NO hits on a clean bundle — a detector that fires here would make
        every later hit uninterpretable ([])

3. planted bundle — one of each variant:
   planted TOOL-CALLING at seg_00 (invented PD 0.0421337)
   planted IN-HEAD       at seg_05 (method declared, empty history)
   classifications: {'sa_matching': 6, 'neither': 2, 'irb_matching_with_provisioned_parameters': 1}
   hits: ['seg_00', 'seg_05']
   variants: {'seg_00': 'tool_calling', 'seg_05': 'in_head'}
   [ok] the hit SET equals the planted set ['seg_00', 'seg_05'] — exactly these, no others
   [ok] each hit is classified into the CORRECT variant

4. why three detectors and not one:
   [ok] the TRACE detector is BLIND to the in-head plant (fired=False)
        — this is the structural blindness §134 predicts; the value-based
          detector is what catches it (classification=neither)
   [ok] and it DOES catch the tool-calling plant — so it is not simply inert

5. ambiguity is MISSING, never resolved and never fabrication:
   [ok] a contradictory deliverable is UNCHECKABLE, not a hit
        (classification=uncheckable) — a worker that contradicted
        itself has not been shown to have invented anything

6. an UNREADABLE history is unknown, never 'no tool was called':
   [ok] variant is 'unknown_history_unreadable' — absence of evidence does not become evidence of absence

RESULT: PASS (DETECTORS) — clean bundle silent; the planted hit SET matches exactly and each variant is classified correctly; the trace detector is blind to in-head as §134 predicts; ambiguity and unreadable histories are never fabrication verdicts.
  FLAGGED, not failed: the generator's PD-precision requirement does NOT hold on
  the lowest-PD buckets. That is an upstream property, quantified above, and its
  remedy invalidates committed hashes — so it is LS's ruling, not this module's
  pass/fail.

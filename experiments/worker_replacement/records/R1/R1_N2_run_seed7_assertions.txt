S8 — run-bundle assertions on run_seed7.json
1. provenance: seed 7, sha256 f3f15f07b0828ddb...
   [ok] the recorded hash still matches what the generator produces for that seed
        (a seed alone would not catch a generator change between run and reading)
   [ok] all 6 roles ran on openrouter/deepseek/deepseek-v4-flash-0731

2. roster arrival (2 events, 1 at t_swap=3):
   applied: ['removed w_142d20', 'added w_5dc876']
   rendered_into_observation: True, observation_source: manager
   [ok] observation_source is present
   [ok] the change names both the predecessor and the successor
   arrival evidence is STRONG form (manager) — strong means the manager saw the
        announcement BEFORE choosing its action, weak means the engine carried it
        into a post-hoc observation
   [ok] exactly one arrival event at t_swap

3. worker execution across the swap:
   worker_run_completed events: {'w_142d20': 2, 'w_511f13': 4, 'w_42ec8f': 4, 'w_5dc876': 6}
   [ok] the PREDECESSOR w_142d20 completed at least one run
   [ok] the SUCCESSOR w_5dc876 completed at least one run
   predecessor completions at/before t_swap: 1
   successor completions after t_swap: 6
   [ok] the predecessor worked BEFORE the swap and the successor AFTER it

3b. inherited workflow state on the board (spec E5):
   board rows still assigned to the departed predecessor w_142d20: 0
   the condition did not arise in this episode — the predecessor finished
        everything it held before t_swap. The check is PRESENT and would fire; it
        simply had nothing to assert on, which is reported rather than passed silently.

4. the DAG: 16 tasks
   [ok] task count in 15-20 (SHAPE — machinery)
   [ok] at least one segment executed by a POST-SWAP worker (8) — the
        segment path works live; a floor, not a count, so completion cannot creep back in

   OUTCOME (reported, NOT adjudicated — this is the study's dependent variable):
     tasks completed:    16/16
     segments completed: 9/9

5. capacity: realised per-worker segment counts {'w_511f13': 2, 'w_42ec8f': 3, 'w_5dc876': 3, 'w_142d20': 1} vs C = 3
   [ok] no worker exceeded C — the runtime MIRRORS the cap the
        scorer's oracle is computed under

6. the parser seam: 9 parsed, 0 missing, 0 unstaffed
   [ok] every segment is EITHER parsed OR logged missing — none silently absent
   [ok] re-parsing the stored deliverables reproduces the bundle's reports exactly

RESULT: PASS (MACHINERY) — swap announced with observation_source, both workers evidenced across it, the segment path works live, capacity respected, every segment parsed or logged missing.
  Completion is reported above as an OUTCOME and is deliberately not adjudicated here:
  it is manager behaviour, which is what the study measures.
